import s from "./Header.module.css"
const Header = () => { 
    return (
        <header className={s.header}>
            <p className={s.text}>Name company</p>
        </header>
    )
}

export default Header